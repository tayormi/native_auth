<?xml version="1.0" encoding="utf-8"?>
<!-- Generated by dartnative_splash. Do not edit manually. -->
<!-- Android 12+ (API 31+): the OS SplashScreen API draws the logo. Pin the
     splash background + icon and DO NOT paint launch_background here — painting
     it would show a second, larger logo after the system splash (the size
     "jump"). windowBackground is the solid splash color so a custom
     AppCompatActivity host (which does not auto-swap to NormalTheme) does not
     flash a default background after the splash. See docs/development.md. -->
<resources>
    <style name="LaunchTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#000000</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
    </style>
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
