import UIKit
import dartnative_ios

@objc class SceneDelegate: DartNativeSceneDelegate {
  // The base class builds the window and starts the DartNative runtime.
  // Override scene(_:willConnectTo:options:) here to customize.
}
